<Window x:Class="SigstreamTelemetryAgent.Views.Dialogs.BrandedDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Icon="pack://application:,,,/Assets/Icons/SigStream.ico"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
        MinWidth="380" MaxWidth="520">

    <!-- ADD THIS -->
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Border Padding="16" CornerRadius="12">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Image Grid.Column="0" Width="40" Height="40"
             Source="pack://application:,,,/Assets/Icons/SigStream.png"
             Stretch="Uniform" />

            <StackPanel Grid.Column="2" Orientation="Vertical" Margin="0,2,0,0">
                <TextBlock Text="{Binding Header}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Margin="0,0,0,12"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="SecondaryBtn" Width="92" Height="30" Margin="0,0,8,0"
                  Visibility="{Binding HasSecondary, Converter={StaticResource BoolToVis}}"
                  Content="{Binding SecondaryText}" IsDefault="False" IsCancel="True" Click="Secondary_Click"/>
                    <Button x:Name="PrimaryBtn" Width="92" Height="30"
                  Content="{Binding PrimaryText}" IsDefault="True" Click="Primary_Click"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
